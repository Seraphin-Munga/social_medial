import { StyleSheet } from "react-native";

export const buttons = StyleSheet.create({
    loginButton: {
        backgroundColor: '#FF7043',
        padding: 10,
        borderRadius: 20,
        marginTop: 25,
        width: '80%',
        alignItems: 'center',
      },
      buttonText: {
        color: '#fff',
      },
});
